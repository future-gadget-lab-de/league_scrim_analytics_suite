pages:
  stage: deploy_doc
  rules:
    - if: $CI_COMMIT_TAG
  tags:
    - doc
  image: alpine:3.20
  needs: ["sphinx"]
  script:
    - echo "Publishing Sphinx site from public/"
  artifacts:
    paths:
      - public
